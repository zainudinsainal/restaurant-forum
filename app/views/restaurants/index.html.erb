<div class="container">
  <div class="row">
  <ul class="nav-pills pl-auto">
    <li class="nav-item"><%= link_to "All restaurant", root_path, class: 'nav-link active' %></li>
    <% @categories.each do |category| %>
      <li class="nav-item"><%= link_to category.name, category_path(category), class: 'nav-link' %></li>
    <% end %>
  </ul>

  <%= render partial: "shared/restaurant_list" %>

  </div>
</div>
