<ul>

  <% @users.each do |user| %>
    <li>
      <%= user.name %>
      <!-- http://localhost:3000/followships?following_id={user.id} -->
      <%= link_to 'Follow', followships_path(following_id: user), method: :post %>
    </li>
  <% end %>

</ul>
